/*
 * Publication Messages
 *
 * This contains all the text for the Publication container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Publication';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Publication container!',
  },
});
