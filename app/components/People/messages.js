/*
 * People Messages
 *
 * This contains all the text for the People component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.People';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the People component!',
  },
});
