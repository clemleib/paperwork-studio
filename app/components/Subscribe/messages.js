/*
 * Subscribe Messages
 *
 * This contains all the text for the Subscribe component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Subscribe';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Subscribe component!',
  },
});
